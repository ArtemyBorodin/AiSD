# # Переменные — существительные
# # Функция — глагол
# # total_sum понятнее, чем s
# # number лучше, чем i
#
# # Задание 1 — Подсчёт чётных чисел
# # Было:
# def counting_even_numbers(list_number):
#     """Считает количество чётных чисел в списке."""
#     count = 0
#     for num in list_number:
#         if num % 2 == 0:
#             count += 1
#     return count
# # 📌 Что делает функция?
# # → Считает количество чётных чисел в списке.
# # Задача:
# # переименовать функцию
# # переименовать аргументы
# # переименовать переменные внутри
#
# # Задание 2 — Поиск максимума
# # Было:
# def search_maximum_number(list_number):
#     """Возвращает максимальное число из списка."""
#     max_number = list_number[0]
#     for num in list_number:
#         if num > max_number:
#             max_number = num
#     return max_number
# # 📌 Что делает функция?
# # → Возвращает максимальное число из списка.
# # Задача:
# # переименовать всё корректно
# # заменить однобуквенные имена
#
# # Задание 3 — Фильтрация и преобразование
# # Было:
# def filter_convert(list_number, threshold):
#     """Возвращает список чисел больше порога, умноженных на два."""
#     result = []
#     for element in list_number:
#         if element > threshold:
#             result.append(element * 2)
#     return result
# # 📌 Что делает функция?
# # → Возвращает список чисел больше порога, умноженных на 2.
# # Задача:
# # сделать имя функции глаголом
# # назвать аргументы понятно
# # назвать список результата корректно

# 🔹 Задание 4 — Работа со строками
# Было:
def return_the_longest_string(list_string):
    """Функция, которая возвращает самую длинную строку из списка
    Args:
        list_string(list): список строк
        sentence_max(str): пустая строка
        sentence(str): переменная цикла
    Returns:
        str: Возвращает самую длинную строку из списка
    """
    sentence_max = ""
    for sentence in list_string:
        if len(sentence) > len(sentence_max):
            sentence_max = sentence
    return sentence_max
# 📌 Что делает функция?
# → Возвращает самую длинную строку из списка.
# Задача:
# улучшить имена
# убрать однобуквенные переменные

print(return_the_longest_string.__doc__)